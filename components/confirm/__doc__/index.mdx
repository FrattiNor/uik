import CodeBox from '@/components/code-box2'
import CodeWrapper from '@/components/code-wrapper'
import ALink from '@/components/a-link'

# Confirm 确认弹窗

点击元素，弹出气泡式的确认框。

## 何时使用

操作需要进一步的确认时，在目标元素附近弹出浮层再次确认。

## 代码演示

<CodeWrapper>
    <CodeBox
        title="基础"
        getComponent={() => import('./demo-normal')}
        getDesc={() => import('./demo-normal/desc.md')}
        getJs={() => import('!raw-loader!./demo-normal')}
    />
    <CodeBox
        title="定位"
        getComponent={() => import('./demo-position')}
        getDesc={() => import('./demo-position/desc.md')}
        getJs={() => import('!raw-loader!./demo-position')}
        getLess={() => import('!raw-loader!./demo-position/index.less')}
    />
</CodeWrapper>

## API

<div className="api">

| 属性           | 说明                 | 类型                | 默认值 | 版本 |
| :------------- | :------------------- | :------------------ | :----- | :--- |
| content        | 内容                 | string \| ReactNode | -      | -    |
| okBtnText      | 弹窗确认按钮的 text  | 确认                | -      | -    |
| cancelBtnText  | 弹窗取消按钮的 text  | 取消                | -      | -    |
| okBtnProps     | 弹窗确认按钮的 props | -                   | -      | -    |
| cancelBtnProps | 弹窗取消按钮的 props | -                   | -      | -    |
| onConfrim      | 确认按钮 点击回调    | function(e)         | -      | -    |
| onCancel       | 取消按钮 点回回调    | function(e)         | -      | -    |

</div>

## 通用 API

<ALink pathname="/uik/tooltip" hash="srcoll_通用API">
    通用API详见Tooltip页
</ALink>
