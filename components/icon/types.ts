import { CSSProperties } from 'react'

export type iconProps = {
    name: string
    className?: string
    style?: CSSProperties
    [key: string]: any
}

export type iconConfig = {
    fontFamily: string
}
