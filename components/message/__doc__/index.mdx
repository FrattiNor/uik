import CodeBox from '@/components/code-box2'
import CodeWrapper from '@/components/code-wrapper'

# message 消息提示

用于全局展示操作的反馈信息。

## 何时使用

提供成功、警告、错误、提醒反馈信息。

边缘显示并自动消失，是一种不打断操作的轻量级提示方式。

## 代码演示

<CodeWrapper>
    <CodeBox
        title="普通提示"
        getComponent={() => import('./demo-normal')}
        getDesc={() => import('./demo-normal/desc.md')}
        getJs={() => import('!raw-loader!./demo-normal')}
    />
    <CodeBox
        title="特殊提示类型"
        getComponent={() => import('./demo-type')}
        getDesc={() => import('./demo-type/desc.md')}
        getJs={() => import('!raw-loader!./demo-type')}
        getLess={() => import('!raw-loader!./demo-type/index.less')}
    />
    <CodeBox
        title="定位"
        getComponent={() => import('./demo-position')}
        getDesc={() => import('./demo-position/desc.md')}
        getJs={() => import('!raw-loader!./demo-position')}
        getLess={() => import('!raw-loader!./demo-position/index.less')}
    />
</CodeWrapper>

## API

组件提供了一些方法：

-   `message.open(title, options)`
-   `message.config(config)`
-   `message.destory()`

<div className="api">

| 属性  | 说明     | 类型                | 默认值 | 版本 |
| :---- | :------- | :------------------ | :----- | :--- |
| title | 反馈信息 | string \| ReactNode | -      | -    |

</div>

`options` 对象属性如下：

<div className="api">

| 属性      | 说明                                                                           | 类型                                                                          | 默认值    | 版本 |
| :-------- | :----------------------------------------------------------------------------- | :---------------------------------------------------------------------------- | :-------- | :--- |
| type      | 消息类型                                                                       | success \| error \| warn \| info \| default                                   | default   | -    |
| id        | 消息的唯一标识，用于 destory 关闭特定 message                                  | number                                                                        | -         | -    |
| position  | 弹出消息的位置                                                                 | topLeft \| topCenter \| topRight \| bottomLeft \| bottomCenter \| bottomRight | topCenter | -    |
| duration  | 弹出消息显示时长秒数`（设为 0 时不自动关闭）` 覆盖全局 message 配置的 duration | number                                                                        | 3         | -    |
| showClose | 显示关闭按钮                                                                   | boolean                                                                       | false     |
| desc      | 对弹出消息的描述                                                               | string \| ReactNode                                                           | -         | -    |
| width     | 弹出消息的宽度（限长后可能导致 title 换行）                                    | number                                                                        | -         | -    |

</div>

`config` 对象属性如下：

<div className="api">

| 属性        | 说明                                              | 类型    | 默认值   | 版本 |
| :---------- | :------------------------------------------------ | :------ | :------- | :--- |
| maxCount    | 最大弹出消息数量                                  | number  | Infinity | -    |
| duration    | 弹出消息显示时长秒数`（设为 0 时不自动关闭）`     | number  | 3        | -    |
| overAnimate | 超出 maxCount 后的新增的 message 是否需要 animate | boolean | false    | -    |

</div>
